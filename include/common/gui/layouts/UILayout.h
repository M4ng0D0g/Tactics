#pragma once

#include "components/base/GUIComponent.h"
#include <vector>
#include <memory>

class UILayout {
public:
	enum LayoutType = { HORIZONTAL, VERTICAL };

	static void applyLayout(std::vector<std::shared_ptr<GUIComponent>>& components, ) {

	}
};

#endif